import { MagicWandIcon } from '../../common/image/Icon';

const FilterTitle = () => {
    return (
        <div className='filter-icon'>
            <MagicWandIcon />
            Filters
        </div>
    );
};

export default FilterTitle;
